spring:
  application:
    name: Pixelate API
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
      enabled: true

server:
  port: 8080

app:
  processing:
    max-batch-size: 20